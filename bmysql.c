#include <stdio.h>
//#include <mysql.h>
/*
MYSQL *con;

int mysql_connect()
{
    con = mysql_init(NULL);
    if(conn == NULL)
    {
        fprintf(stderr, "%s\n", mysql_error(con));
        return 1;
    }
    if(mysql_real_connect(con, "localhost", "probank", "mettwurst", NULL, 0, NULL, 0) == NULL)
    {
        fprintf(stderr, "%s\n", mysql_error(con));
        mysql_close(con);
        return 1;
    }
}
*/
